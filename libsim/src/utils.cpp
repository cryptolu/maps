/******************************************************************************
 *
 * utilities
 *
 ******************************************************************************/

#include <cstdint>

unsigned int bit_count(uint32_t x)
{
    unsigned int h = 0;
    uint32_t u = x;
    while (u > 0)
    {
        h += (u & 1);
        u >>= 1;
    }
    return h;
}
